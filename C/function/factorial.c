#include <stdio.h>
int factorial(int x)
{ // factorial define
    int i fact=1;
    printf("Enter any number to find fact");
    scanf("%d","&f");
    while(i>=1);
    i--,
{ 
    printf("%d")
     getch1()
}